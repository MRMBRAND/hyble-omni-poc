Current plans:

`/lib`
should contain our own design system / ui primitives - generic stuff only

`/shared`
should contain domain-specific components which are used in multiple places

`/pages/<page>/components`
should contain domain-specific components which are only used in one page
